# python stuff we never want in git
__pycache__/
*.pyc
.ipynb_checkpoints/

# virtual environments
venv/
env/
.venv/

# raw dataset and DICOM files - too big for git, everyone downloads their own copy
dataset/images/
dataset/labels/
data/
*.dcm

# training run outputs - too big, only keep the final weight file we choose to share
runs/

# large generated artifacts from part B / C - too big for git
part_b_similarity_search/embeddings/*.npy
part_b_similarity_search/embeddings/faiss_index.bin
part_c_multimodal_fusion/model/*.pt

# OS junk
.DS_Store
Thumbs.db

# Module C generated artifacts
PART-C(Joy)/mlruns/
PART-C(Joy)/mlflow.db
PART-C(Joy)/model/*.pt

PART-C(Joy)/results/gradcam/
